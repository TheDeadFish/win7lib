
remove_definitions(-D_WIN32_WINNT=0x502 -DWINVER=0x502)
add_definitions(-D_WIN32_WINNT=0x600 -DWINVER=0x600)

add_definitions(-D_KERNEL32_)

list(APPEND SOURCE
    sync.c
    )

add_library(kernel32_vista STATIC ${SOURCE})
